<div class="space-y-6">
  <div class="sm:flex sm:items-center sm:justify-between">
    <h1 class="text-xl sm:text-2xl font-bold text-gray-900 dark:text-white">
      <%= t("admin.curator_applications.application_from", username: @application.user.username) %>
    </h1>
    <div class="mt-3 sm:mt-0">
      <%= link_to t("admin.curator_applications.back_to_list"), curator_admin_curator_applications_path,
          class: "inline-flex items-center rounded-md bg-white dark:bg-gray-800 px-3 py-2 text-sm font-semibold text-gray-900 dark:text-white shadow-sm ring-1 ring-inset ring-gray-300 dark:ring-gray-600 hover:bg-gray-50 dark:hover:bg-gray-700" %>
    </div>
  </div>

  <!-- Status and Actions -->
  <div class="overflow-hidden rounded-lg bg-white dark:bg-gray-800 shadow">
    <div class="px-4 py-4 sm:p-6">
      <div class="flex flex-col sm:flex-row sm:items-start sm:justify-between gap-4">
        <div class="space-y-2">
          <div>
            <span class="text-sm text-gray-500 dark:text-gray-400">Status:</span>
            <% case @application.status %>
            <% when "pending" %>
              <span class="ml-2 inline-flex items-center rounded-full bg-yellow-100 dark:bg-yellow-900/40 px-3 py-1 text-sm font-medium text-yellow-800 dark:text-yellow-300">Pending</span>
            <% when "approved" %>
              <span class="ml-2 inline-flex items-center rounded-full bg-green-100 dark:bg-green-900/40 px-3 py-1 text-sm font-medium text-green-800 dark:text-green-300">Approved</span>
            <% when "rejected" %>
              <span class="ml-2 inline-flex items-center rounded-full bg-red-100 dark:bg-red-900/40 px-3 py-1 text-sm font-medium text-red-800 dark:text-red-300">Rejected</span>
            <% end %>
          </div>
          <p class="text-sm text-gray-500 dark:text-gray-400">Submitted: <%= @application.created_at.strftime("%d.%m.%Y %H:%M") %></p>
        </div>

        <% if @application.pending? %>
          <div class="flex flex-col gap-3">
            <%= form_with url: approve_curator_admin_curator_application_path(@application), method: :post, class: "flex flex-col gap-2" do |f| %>
              <%= f.submit "Approve",
                  class: "inline-flex items-center justify-center rounded-md bg-green-600 px-4 py-2 text-sm font-semibold text-white shadow-sm hover:bg-green-500 cursor-pointer" %>
            <% end %>

            <%= form_with url: reject_curator_admin_curator_application_path(@application), method: :post, class: "flex flex-col gap-2" do |f| %>
              <%= f.text_field :admin_notes,
                  placeholder: t("admin.curator_applications.rejection_reason"),
                  class: "rounded-md border-gray-300 dark:border-gray-600 dark:bg-gray-700 dark:text-white text-sm w-full" %>
              <%= f.submit "Reject",
                  class: "inline-flex items-center justify-center rounded-md bg-red-600 px-4 py-2 text-sm font-semibold text-white shadow-sm hover:bg-red-500 cursor-pointer" %>
            <% end %>
          </div>
        <% end %>
      </div>

      <% if @application.reviewed_by.present? %>
        <div class="mt-4 text-sm text-gray-500 dark:text-gray-400">
          <%= t("admin.curator_applications.reviewed_by",
              admin: @application.reviewed_by.username,
              date: @application.reviewed_at.strftime("%d.%m.%Y %H:%M")) %>
        </div>
      <% end %>
    </div>
  </div>

  <!-- Application Details -->
  <div class="overflow-hidden rounded-lg bg-white dark:bg-gray-800 shadow">
    <div class="px-4 py-5 sm:p-6">
      <dl class="space-y-6">
        <div>
          <dt class="text-sm font-medium text-gray-500 dark:text-gray-400">Motivation</dt>
          <dd class="mt-1 text-sm text-gray-900 dark:text-white whitespace-pre-wrap bg-gray-50 dark:bg-gray-700 p-4 rounded-lg"><%= @application.motivation %></dd>
        </div>

        <% if @application.experience.present? %>
          <div>
            <dt class="text-sm font-medium text-gray-500 dark:text-gray-400">Experience</dt>
            <dd class="mt-1 text-sm text-gray-900 dark:text-white whitespace-pre-wrap bg-gray-50 dark:bg-gray-700 p-4 rounded-lg"><%= @application.experience %></dd>
          </div>
        <% end %>
      </dl>
    </div>
  </div>

  <% if @application.admin_notes.present? %>
    <div class="overflow-hidden rounded-lg bg-white dark:bg-gray-800 shadow">
      <div class="px-4 py-5 sm:p-6">
        <h3 class="text-base font-medium text-gray-900 dark:text-white mb-4"><%= t("admin.curator_applications.admin_notes") %></h3>
        <div class="text-sm text-gray-900 dark:text-white whitespace-pre-wrap bg-gray-50 dark:bg-gray-700 p-4 rounded-lg">
          <%= @application.admin_notes %>
        </div>
      </div>
    </div>
  <% end %>
</div>
