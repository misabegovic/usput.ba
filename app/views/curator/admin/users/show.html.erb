<div class="space-y-6">
  <div class="sm:flex sm:items-center sm:justify-between">
    <h1 class="text-xl sm:text-2xl font-bold text-gray-900 dark:text-white"><%= @user.username %></h1>
    <div class="mt-3 sm:mt-0 flex flex-col sm:flex-row gap-2">
      <%= link_to t("admin.users.edit"), edit_curator_admin_user_path(@user),
          class: "inline-flex items-center justify-center rounded-md bg-indigo-600 px-3 py-2 text-sm font-semibold text-white shadow-sm hover:bg-indigo-500" %>
      <%= link_to t("admin.users.back_to_list"), curator_admin_users_path,
          class: "inline-flex items-center justify-center rounded-md bg-white dark:bg-gray-800 px-3 py-2 text-sm font-semibold text-gray-900 dark:text-white shadow-sm ring-1 ring-inset ring-gray-300 dark:ring-gray-600 hover:bg-gray-50 dark:hover:bg-gray-700" %>
    </div>
  </div>

  <div class="overflow-hidden rounded-lg bg-white dark:bg-gray-800 shadow">
    <div class="px-4 py-4 sm:p-6">
      <dl class="grid grid-cols-1 gap-x-4 gap-y-4 sm:gap-y-6 sm:grid-cols-2">
        <div>
          <dt class="text-sm font-medium text-gray-500 dark:text-gray-400"><%= t("admin.users.username") %></dt>
          <dd class="mt-1 text-sm text-gray-900 dark:text-white"><%= @user.username %></dd>
        </div>
        <div>
          <dt class="text-sm font-medium text-gray-500 dark:text-gray-400"><%= t("admin.users.type") %></dt>
          <dd class="mt-1">
            <span class="inline-flex items-center rounded-full px-2.5 py-0.5 text-xs font-medium
              <%= case @user.user_type
                  when 'basic' then 'bg-gray-100 dark:bg-gray-700 text-gray-800 dark:text-gray-300'
                  when 'curator' then 'bg-emerald-100 dark:bg-emerald-900/40 text-emerald-800 dark:text-emerald-300'
                  when 'admin' then 'bg-indigo-100 dark:bg-indigo-900/40 text-indigo-800 dark:text-indigo-300'
                  end %>">
              <%= t("admin.users.types.#{@user.user_type}") %>
            </span>
          </dd>
        </div>
        <div>
          <dt class="text-sm font-medium text-gray-500 dark:text-gray-400"><%= t("admin.users.created_at") %></dt>
          <dd class="mt-1 text-sm text-gray-900 dark:text-white"><%= @user.created_at.strftime("%d.%m.%Y %H:%M") %></dd>
        </div>
        <div>
          <dt class="text-sm font-medium text-gray-500 dark:text-gray-400"><%= t("admin.users.updated_at") %></dt>
          <dd class="mt-1 text-sm text-gray-900 dark:text-white"><%= @user.updated_at.strftime("%d.%m.%Y %H:%M") %></dd>
        </div>
      </dl>
    </div>
  </div>

  <% if @user.spam_blocked? %>
    <div class="rounded-lg border-2 border-red-200 dark:border-red-800 bg-red-50 dark:bg-red-900/30">
      <div class="px-4 py-4 sm:p-6">
        <h3 class="text-base font-medium text-red-900 dark:text-red-300 mb-2">User is Blocked</h3>
        <p class="text-sm text-red-700 dark:text-red-400 mb-2">Blocked at: <%= @user.spam_blocked_at&.strftime("%d.%m.%Y %H:%M") %></p>
        <p class="text-sm text-red-700 dark:text-red-400 mb-2">Blocked until: <%= @user.spam_blocked_until&.strftime("%d.%m.%Y %H:%M") %></p>
        <p class="text-sm text-red-700 dark:text-red-400 mb-4">Reason: <%= @user.spam_block_reason %></p>

        <%= form_with url: unblock_curator_admin_user_path(@user), method: :post do |f| %>
          <button type="submit"
                  class="inline-flex items-center justify-center rounded-md bg-green-600 px-4 py-2 text-sm font-semibold text-white shadow-sm hover:bg-green-500 focus:outline-none focus:ring-2 focus:ring-green-500 focus:ring-offset-2">
            Unblock User
          </button>
        <% end %>
      </div>
    </div>
  <% end %>

  <% if @user.curator? && @activities.present? %>
    <div class="overflow-hidden rounded-lg bg-white dark:bg-gray-800 shadow">
      <div class="px-4 py-4 sm:p-6">
        <h3 class="text-base font-medium text-gray-900 dark:text-white mb-4">Recent Activities</h3>
        <ul class="space-y-3">
          <% @activities.each do |activity| %>
            <li class="text-sm text-gray-600 dark:text-gray-400">
              <span class="font-medium text-gray-900 dark:text-white"><%= activity.action %></span>
              <span class="text-gray-500 dark:text-gray-400">- <%= activity.created_at.strftime("%d.%m.%Y %H:%M") %></span>
            </li>
          <% end %>
        </ul>
      </div>
    </div>
  <% end %>
</div>
