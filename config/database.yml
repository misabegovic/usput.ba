# PostgreSQL configuration
#
# Two databases:
# - primary: Main application database
# - queue: Solid Queue for background jobs
#
default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  primary:
    <<: *default
    <% if ENV["PROD_DATABASE_URL"].to_s.strip.length > 0 %>
    url: <%= ENV["PROD_DATABASE_URL"] %>
    <% else %>
    database: klosaer_development
    username: <%= ENV.fetch("POSTGRES_USER") { "postgres" } %>
    password: <%= ENV.fetch("POSTGRES_PASSWORD") { "" } %>
    host: <%= ENV.fetch("POSTGRES_HOST") { "localhost" } %>
    port: <%= ENV.fetch("POSTGRES_PORT") { 5432 } %>
    <% end %>
  queue:
    <<: *default
    <% if ENV["PROD_QUEUE_DATABASE_URL"].to_s.strip.length > 0 %>
    url: <%= ENV["PROD_QUEUE_DATABASE_URL"] %>
    <% else %>
    database: klosaer_queue_development
    username: <%= ENV.fetch("POSTGRES_USER") { "postgres" } %>
    password: <%= ENV.fetch("POSTGRES_PASSWORD") { "" } %>
    host: <%= ENV.fetch("POSTGRES_HOST") { "localhost" } %>
    port: <%= ENV.fetch("POSTGRES_PORT") { 5432 } %>
    <% end %>
    pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 10 } %>
    migrations_paths: db/queue_migrate

test:
  primary:
    <<: *default
    database: klosaer_test
    username: <%= ENV.fetch("POSTGRES_USER") { "postgres" } %>
    password: <%= ENV.fetch("POSTGRES_PASSWORD") { "" } %>
    host: <%= ENV.fetch("POSTGRES_HOST") { "localhost" } %>
    port: <%= ENV.fetch("POSTGRES_PORT") { 5432 } %>
  queue:
    <<: *default
    database: klosaer_queue_test
    pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 10 } %>
    username: <%= ENV.fetch("POSTGRES_USER") { "postgres" } %>
    password: <%= ENV.fetch("POSTGRES_PASSWORD") { "" } %>
    host: <%= ENV.fetch("POSTGRES_HOST") { "localhost" } %>
    port: <%= ENV.fetch("POSTGRES_PORT") { 5432 } %>
    migrations_paths: db/queue_migrate

production:
  primary:
    <<: *default
    url: <%= ENV["DATABASE_URL"] %>
  queue:
    <<: *default
    url: <%= ENV["QUEUE_DATABASE_URL"] %>
    pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 10 } %>
    migrations_paths: db/queue_migrate
